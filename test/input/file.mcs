function hello {
	execute @a ~ ~ ~ {
		var hai = helloWorld
		execute @p ~ ~ ~ {
			var hey = 15
			$hey = 10
			$hai = ALLAL
			function($hey);

			function($hai)
			testfor @a[score_hello_world] 223dsa $hey $hai
		}
		function($hai)
		$hai = 10
	}
	function(test)
}

function hi {
	 function(check)
}
